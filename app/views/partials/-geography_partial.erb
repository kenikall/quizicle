<div class="container">
  <div class="jumbotron">
    <h1>
    <img src="<%= @problem. %>">
    <%=@problem = Problem.find_by(subject_id: Subject.find_by(name: "geography")).sample%>
    </h1>
    <% if session[:id] %>
    <form id="answer" action="/students/<%= session[:id] %>" method="post">
      <input type="text" name=<%="@problem.response"%>>
      <input type="submit" value="answer">
    </form>
    <% else %>
    <form class="answer" action="/quizzes/<%=@quiz.id%>" method="post">
      <input type="text" name="response">
      <input type="submit" value="answer">
    </form>
    <% end %>
  </div>
</div>
